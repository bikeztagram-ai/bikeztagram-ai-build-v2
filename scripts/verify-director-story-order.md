# Director story-order regression

`verify-director-story-order.mjs` proves that selected moments retain the Director's editorial sequence instead of being reordered by source-media timestamps. The regression checks hook/hero roles, stable story positions, reveal/action coverage and deterministic sequence metadata.
