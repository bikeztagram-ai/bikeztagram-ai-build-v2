import assert from 'node:assert/strict';
import { OUTPUT_PRESETS,resolveOutputPreset,outputPlanFields } from '../src/outputPresets.js';
assert.deepEqual([OUTPUT_PRESETS.portrait.width,OUTPUT_PRESETS.portrait.height],[1080,1920]);assert.deepEqual([OUTPUT_PRESETS.square.width,OUTPUT_PRESETS.square.height],[1080,1080]);assert.deepEqual([OUTPUT_PRESETS.landscape.width,OUTPUT_PRESETS.landscape.height],[1920,1080]);assert.deepEqual([OUTPUT_PRESETS.story.width,OUTPUT_PRESETS.story.height],[1080,1920]);assert.deepEqual([OUTPUT_PRESETS.cinema.width,OUTPUT_PRESETS.cinema.height],[1920,804]);
assert.equal(resolveOutputPreset(undefined,'make this widescreen for youtube').id,'landscape');assert.equal(resolveOutputPreset(undefined,'make a square feed post').id,'square');assert.equal(resolveOutputPreset(undefined,'make a cinematic 2.39 widescreen film').id,'cinema');assert.equal(resolveOutputPreset(undefined,'make an instagram story').id,'story');assert.equal(resolveOutputPreset('portrait','widescreen').id,'portrait');
for(const id of Object.keys(OUTPUT_PRESETS)){const plan=outputPlanFields(id);assert.equal(plan.outputPreset,id);assert.equal(plan.outputWidth,OUTPUT_PRESETS[id].width);assert.equal(plan.outputHeight,OUTPUT_PRESETS[id].height);assert.equal(plan.outputAspectRatio,OUTPUT_PRESETS[id].aspectRatio);assert.equal(plan.outputFps,OUTPUT_PRESETS[id].fps);}
console.log('Universal output preset contract: PASS — portrait, story, square, landscape and cinema.');
