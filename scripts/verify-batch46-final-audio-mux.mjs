import assert from 'node:assert/strict';
import fs from 'node:fs';
const mux=fs.readFileSync(new URL('../src/finalAudioMux.js',import.meta.url),'utf8');
const loop=fs.readFileSync(new URL('../src/renderQualityLoop.js',import.meta.url),'utf8');
const app=fs.readFileSync(new URL('../src/App.jsx',import.meta.url),'utf8');
assert.match(mux,/captureStream/);
assert.match(mux,/createMediaStreamDestination/);
assert.match(mux,/createBufferSource/);
assert.match(mux,/MediaRecorder/);
assert.match(mux,/audioTrack/);
assert.match(loop,/attachGeneratedAudioToVideo/);
assert.match(loop,/finalAudioAttached/);
assert.match(loop,/audioDataUrl/);
assert.match(app,/renderPlan\.music=\{\.\.\.musicResult\.soundtrack\}/);
console.log('batch46-final-audio-mux: PASS');
