import assert from 'node:assert/strict';
import fs from 'node:fs';
const file=fs.readFileSync(new URL('../src/apiRequest.js',import.meta.url),'utf8');
assert.match(file,/RETRYABLE/);
assert.match(file,/retryableHttp/);
assert.match(file,/RETRYABLE\.has\(error\.status\)/);
assert.match(file,/retryableNetwork/);
assert.match(file,/attempt >= maxAttempts/);
assert.match(file,/baseDelayMs \* \(2 \*\* \(attempt - 1\)\)/);
console.log('batch53-api-retry-runtime: PASS');
