# Vercel Preview Test Marker

This file intentionally triggers one connected Git → Vercel preview deployment so the current development branch can be tested on Android.

Production is not changed by this marker.
