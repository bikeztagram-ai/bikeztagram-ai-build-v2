export function validateGenerationJobBudget({kind='video',durationSeconds=0,candidates=1,limits={musicMaxSeconds:300,videoMaxSeconds:12,maxCandidates:3} }={}){const max=kind==='music'?limits.musicMaxSeconds:limits.videoMaxSeconds;const errors=[];if(Number(durationSeconds)>max)errors.push('duration-limit');if(Number(candidates)>limits.maxCandidates)errors.push('candidate-limit');return {valid:errors.length===0,errors,limits};}
