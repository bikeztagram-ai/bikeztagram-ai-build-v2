export function validateGeneratedMedia(media={}){const errors=[];if(!media.url&&!media.blob)errors.push('media-source-required');if(!media.mimeType)errors.push('mime-type-required');if(media.duration!=null&&Number(media.duration)<=0)errors.push('invalid-duration');return {valid:errors.length===0,errors};}
export function normaliseGeneratedMedia(media={}){return {id:media.id||`generated-${Date.now()}`,url:media.url||null,blob:media.blob||null,mimeType:media.mimeType||'application/octet-stream',duration:Number(media.duration)||0,width:Number(media.width)||0,height:Number(media.height)||0,source:'generated',modelId:media.modelId||null,metadata:media.metadata||{}};}
