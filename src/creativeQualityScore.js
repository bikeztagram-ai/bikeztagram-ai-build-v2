export function scoreCreativeQuality({ story=0, visual=0, edit=0, audio=0, technical=0, originality=0 }={}) { const values={story,visual,edit,audio,technical,originality}; const weights={story:.22,visual:.20,edit:.18,audio:.14,technical:.12,originality:.14}; const score=Object.keys(weights).reduce((sum,k)=>sum+(Math.max(0,Math.min(1,Number(values[k])||0))*weights[k]),0); return {score:Number(score.toFixed(4)), grade:score>=.9?'A':score>=.8?'B':score>=.7?'C':score>=.6?'D':'F', dimensions:values}; }
