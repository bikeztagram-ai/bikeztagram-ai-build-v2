export async function generateAIMusic({prompt,durationMs=30000,forceInstrumental=false}={}){const response=await fetch('/api/music',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt,durationMs,forceInstrumental})});if(!response.ok){let detail='AI music generation failed.';try{const data=await response.json();detail=data.error||detail;if(data.details)detail+=` ${data.details}`}catch{}throw new Error(detail)}const blob=await response.blob();if(!blob.size)throw new Error('AI music provider returned an empty audio file.');return{blob,mimeType:blob.type||'audio/mpeg',songId:response.headers.get('X-Bikeztagram-Music-Song-Id')||'',provider:'Eleven Music v2',original:true};}
