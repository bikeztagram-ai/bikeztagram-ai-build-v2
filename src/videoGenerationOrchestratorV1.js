/* Model-neutral video orchestration for arbitrary creative scenes. */
export function buildVideoGenerationRequest({prompt='',type='custom',subjects=[],assets=[],duration=5,aspectRatio='9:16',camera={},environment={},action={},style={},continuity={}}={}){return {version:'video-generation-request-v1',prompt,type,subjects,assets,duration:Math.max(.5,Math.min(120,Number(duration)||5)),aspectRatio,camera,environment,action,style,continuity,requirements:{preserveSubjects:subjects.length>0,originalWorld:true,temporalConsistency:true}};}
export async function generateVideoCandidates(request,{runtime,candidates=2}={}){if(!runtime)return {status:'fallback-required',candidates:[]};const outputs=[];for(let i=0;i<candidates;i++)outputs.push(await runtime.generateScene({...request,candidate:i+1}));return {status:'generated',candidates:outputs};}
export function rankVideoCandidates(candidates=[]){return [...candidates].sort((a,b)=>Number(b.score||0)-Number(a.score||0)).map((c,i)=>({...c,rank:i+1}));}
