export function evaluateRealBenchmark({results=[],minimumScore=80,minimumPassRate=.8}={}){const scores=results.map(r=>Number(r?.score)).filter(Number.isFinite);const passed=scores.filter(s=>s>=minimumScore).length;const passRate=scores.length?passed/scores.length:0;return {ready:scores.length>0&&passRate>=minimumPassRate,averageScore:scores.length?scores.reduce((a,b)=>a+b,0)/scores.length:0,passRate,passed,total:scores.length};}
