export function directVisualEvents({events=[],shots=[]}={}){return events.filter(e=>['major','medium'].includes(e.impact||'')).map(e=>{const candidates=shots.map((s,i)=>({i,distance:Math.abs((Number(s.start)||0)-(Number(e.time)||0))})).sort((a,b)=>a.distance-b.distance);const hit=candidates[0];return {eventId:e.id||null,shotIndex:hit?.i??null,distance:hit?.distance??null,directive:e.type==='drop'?'reveal-impact':e.type==='build'?'escalate-camera':e.type==='break'?'hold-breathe':'support-cut'};});}
