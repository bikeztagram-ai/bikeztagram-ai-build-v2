/* BIKEZTAGRAM AI — universal local editorial rhythm planner. */
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const n=(v,d=0)=>{const x=Number(v);return Number.isFinite(x)?x:d};
const text=v=>String(v??'').toLowerCase();
function intent(prompt=''){const p=text(prompt);return{fast:/fast|action|energetic|race|chase|aggressive|high energy/.test(p),calm:/calm|slow|peaceful|emotional|beautiful|romantic|ambient/.test(p),trailer:/trailer|teaser|cinematic|commercial|promo/.test(p),dramatic:/dramatic|dark|moody|mysterious|gritty/.test(p)};}
function role(c,i,total){const r=text(c.editorialRole||c.role||c.purpose);if(/hook|opening/.test(r))return'hook';if(/build|setup|approach/.test(r))return'build';if(/reveal|unveil|showcase/.test(r))return'reveal';if(/action|impact|chase/.test(r))return'action';if(/emotional|emotion/.test(r))return'emotional';if(/hero|ending|resolution|final/.test(r))return'hero';if(i===0)return'hook';if(i===total-1)return'hero';return'variation';}
function baseDuration(c,intentValue){const quality=n(c.directorSelectionScore,c.score);const r=role(c,0,1);let d=r==='hook'?.9:r==='hero'?1.7:r==='action'?1.0:r==='reveal'?1.2:r==='emotional'?1.7:1.3;if(quality>180)d+=.15;if(intentValue.fast)d-=.15;if(intentValue.calm)d+=.25;return clamp(d,.65,2.4);}
export function planEditorialRhythm(cuts=[],{targetDuration=15,creativePrompt=''}={}){if(!Array.isArray(cuts)||!cuts.length)return[];const mode=intent(creativePrompt),out=cuts.map((c,i)=>({...c,editorialRole:role(c,i,cuts.length),rhythmIndex:i,rhythmDuration:baseDuration(c,mode)}));let total=out.reduce((s,c)=>s+c.rhythmDuration,0);const target=clamp(n(targetDuration,15),3,60);if(total>0){const factor=clamp(target/total,.72,1.35);out.forEach(c=>{c.rhythmDuration=Number(clamp(c.rhythmDuration*factor,.55,3).toFixed(2));});}const refreshed=out.reduce((s,c)=>s+c.rhythmDuration,0);if(refreshed>0){const correction=target/refreshed;out[out.length-1].rhythmDuration=Number(clamp(out.at(-1).rhythmDuration*correction,.55,3).toFixed(2));}return out.map((c,i)=>{const r=c.editorialRole;let pacing=r==='hook'?'immediate':r==='action'?'driving':r==='reveal'?'controlled':r==='emotional'?'lingering':r==='hero'?'resolved':'building';if(mode.fast&&r==='action')pacing='driving-fast';if(mode.calm&&(r==='build'||r==='emotional'||r==='hero'))pacing='lingering';return{...c,pacing,beatPosition:i/(Math.max(1,out.length-1)),rhythmDuration:c.rhythmDuration};});}
