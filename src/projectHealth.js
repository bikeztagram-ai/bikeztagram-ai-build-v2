export function assessProjectHealth(project={}){const checks={assets:Array.isArray(project.assets)&&project.assets.length>0,intent:Boolean(project.intent),story:Boolean(project.story||project.blueprint),look:Boolean(project.treatment||project.visualLook),edit:Boolean(project.editPlan),output:Boolean(project.outputs||project.output)};const passed=Object.values(checks).filter(Boolean).length;return {ready:passed===Object.keys(checks).length,score:passed/Object.keys(checks).length,checks};}
