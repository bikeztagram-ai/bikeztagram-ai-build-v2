export function planReframe(shot={},output={}){const ratio=output.ratio||'9:16';const subject=shot.subject||{x:.5,y:.5,w:.3,h:.3};const safeX=Math.min(.9,Math.max(.1,subject.x));const captionReserve=output.captions?Math.min(.25,Number(output.captionReserve)||.14):0;return {ratio,subjectAnchor:{x:safeX,y:Math.min(.9,Math.max(.1,subject.y))},captionReserve,mode:output.mode||'cinematic-follow',preserveHeadroom:output.preserveHeadroom!==false};}
export function buildReframeSet(shots=[],outputs=[]){return outputs.flatMap((output)=>shots.map((shot)=>({shotId:shot.id,...planReframe(shot,output)})));}
