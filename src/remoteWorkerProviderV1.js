export function createRemoteWorkerProvider({id,baseUrl,authMode='server-secret',capabilities=[]}={}){return {version:'remote-worker-provider-v1',id,baseUrl,authMode,capabilities,async createJob(job){if(!baseUrl)throw new Error('worker-base-url-required');const r=await fetch(`${baseUrl}/jobs`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(job)});if(!r.ok)throw new Error(`worker-create-job-failed:${r.status}`);return r.json();},async getJob(jobId){const r=await fetch(`${baseUrl}/jobs/${encodeURIComponent(jobId)}`);if(!r.ok)throw new Error(`worker-get-job-failed:${r.status}`);return r.json();}};}
