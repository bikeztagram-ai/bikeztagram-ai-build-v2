export function createAceStepRuntimeAdapter({baseUrl='http://127.0.0.1:8001',fetchImpl=fetch}={}){return {id:'ace-step-1.5',kind:'music',baseUrl,async health(){const r=await fetchImpl(`${baseUrl}/health`);return {ok:r.ok,status:r.status};},async run(command){const r=await fetchImpl(`${baseUrl}/generate`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(command)});if(!r.ok)throw new Error(`ACE-Step request failed: ${r.status}`);return r.json();}};}
