export function directCreativeEvents({brief={},musicEvents=[],scenePlan=[]}={}){const events=[...musicEvents.map(e=>({...e,source:'music'})),...scenePlan.flatMap(s=>(s.events||[]).map(e=>({...e,source:'scene',sceneId:s.id})))].sort((a,b)=>(a.time||0)-(b.time||0));return {version:'creative-event-director-v1',events,actions:events.map(e=>({time:e.time||0,action:e.action||'scene-change',reason:e.reason||brief.prompt||''}))};}
