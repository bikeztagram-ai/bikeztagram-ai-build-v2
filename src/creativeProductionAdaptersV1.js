const REQUIRED=['media-intake','creative-direction','music-direction','scene-placement','continuity','transition-direction','render','qa'];
const aliases={media:'media-intake',director:'creative-direction',music:'music-direction',video:'scene-placement',transitions:'transition-direction',renderer:'render',quality:'qa'};
export function createCreativeProductionAdapters({services={},fallbacks={},strict=true}={}){const handlers={};for(const stage of REQUIRED){const service=services[stage]??services[Object.keys(aliases).find(k=>aliases[k]===stage)];const fallback=fallbacks[stage];if(typeof service==='function')handlers[stage]=async ctx=>service(ctx);else if(!strict&&typeof fallback==='function')handlers[stage]=async ctx=>fallback(ctx);}
 return {version:'creative-production-adapters-v1',handlers,available:Object.fromEntries(REQUIRED.map(s=>[s,typeof handlers[s]==='function'])),missing:REQUIRED.filter(s=>typeof handlers[s]!=='function'),strict};}
export function validateCreativeProductionAdapters(adapters,{allowMissing=[]}={}){const allowed=new Set(allowMissing);const missing=(adapters?.missing||REQUIRED).filter(s=>!allowed.has(s));return missing.length?{ok:false,reason:`missing-production-adapter:${missing[0]}`,missing}:{ok:true};}
