export async function reviseMusicUntilAccepted({generate,score,request,maxAttempts=3,threshold=80}={}){let best=null;for(let attempt=1;attempt<=maxAttempts;attempt++){const output=await generate({...request,attempt,revisionOf:best?.output||null});const result=await score(output,request);const candidate={output,score:Number(result?.score??result)||0,feedback:result?.feedback||[]};if(!best||candidate.score>best.score)best=candidate;if(best.score>=threshold)return {...best,accepted:true,attempt};}return {...best,accepted:false,attempt:maxAttempts};}
