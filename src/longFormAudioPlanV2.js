/* Long-form audio: preserves musical continuity while allowing section-level evolution. */
export function buildLongFormAudioPlan({composition={},duration=0}={}){const total=Number(duration||composition.duration)||0,sections=Array.isArray(composition.sections)?composition.sections:[];return {version:'long-form-audio-v2',duration:total,sections:sections.map((s,i)=>({...s,index:i,fadeIn:i===0?0:.08,fadeOut:i===sections.length-1?.35:.08,carryTheme:i>0,variation:true})),continuity:{sharedTempo:true,sharedTheme:true,sectionHandoffs:true,noForcedLoop:true},export:{fullMix:true,stems:true,master:true}};}
