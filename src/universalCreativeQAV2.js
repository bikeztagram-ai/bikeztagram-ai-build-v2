/* Subject-agnostic creative QA. Technical validity is separate from creative quality. */
const DIMENSIONS=['story','pacing','visualQuality','subjectConsistency','motionQuality','continuity','musicImpact','beatUtilisation','shotVariety','promptFidelity'];
export function scoreUniversalCreativeQA(observed={}){const scores={};for(const d of DIMENSIONS){scores[d]=Math.max(0,Math.min(100,Number(observed[d]??0)));}const values=Object.values(scores);return {version:'universal-creative-qa-v2',dimensions:scores,score:Math.round(values.reduce((a,b)=>a+b,0)/values.length),technicalPass:Boolean(observed.technicalPass),weakest:DIMENSIONS.slice().sort((a,b)=>scores[a]-scores[b]).slice(0,3)};}
export function buildUniversalRevisionRequest(qa,{maxActions=3}={}){return {version:'universal-revision-request-v2',actions:qa.weakest.slice(0,maxActions).map(d=>({dimension:d,action:d==='musicImpact'?'regenerate-music':d==='promptFidelity'?'regenerate-with-clarified-brief':d==='subjectConsistency'?'regenerate-subject-aware':d==='continuity'?'regenerate-bridge':'retune-edit'})),bounded:true};}
