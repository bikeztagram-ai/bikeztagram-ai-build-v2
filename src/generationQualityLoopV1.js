export async function runGenerationQualityLoop({generate,evaluate,revise,request,maxAttempts=3,minimumScore=80}={}){let current=request;const attempts=[];for(let i=0;i<maxAttempts;i++){const output=await generate(current);const evaluation=await evaluate(output,current);attempts.push({attempt:i+1,output,evaluation});if(Number(evaluation?.score)>=minimumScore)return {passed:true,best:output,evaluation,attempts};if(!revise)break;current=await revise({request:current,output,evaluation,attempt:i+1});}return {passed:false,best:attempts.at(-1)?.output||null,evaluation:attempts.at(-1)?.evaluation||null,attempts};}
