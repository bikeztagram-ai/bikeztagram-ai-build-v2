/* Compiles scene specs into adapter-ready requests without selecting a provider. */
export function compileSceneRequest(scene,{capability='image-to-video'}={}){return {version:'runtime-generation-request-v1',capability,prompt:scene.idea||scene.prompt||'',assets:scene.assets||[],subjects:scene.subjects||[],duration:Number(scene.duration)||5,aspectRatio:scene.aspectRatio||'9:16',style:scene.style||{},camera:scene.camera||{},environment:scene.environment||{},action:scene.action||{},audio:scene.audio||{},continuity:scene.continuity||{}};}
export function compileMusicRequest({prompt='',duration=30,style={},structure={},references=[]}={}){return {version:'runtime-music-request-v1',capability:'text-to-music',prompt,duration,style,structure,references,requireEvents:true,originalOnly:true};}
