import {buildMusicGenerationPlan} from './musicCompositionV3.js';
import {createOriginalMusicWav} from './musicProviderV3.js';
export function buildProviderMusicRequest({prompt='',duration=30,genre='cinematic',mood='cinematic',energy=.72,bpm=112,filmType='trailer'}={}){const plan=buildMusicGenerationPlan({prompt,duration,genre,mood,energy,bpm,filmType});return {providerVersion:'music-provider-v2',duration:plan.duration,request:plan.providerRequest,originality:plan.copyright};}
export function validateProviderAudio(result,{expectedDuration=30}={}){const audio=result?.audio||result?.audioUrl||result?.url;const duration=Number(result?.duration||result?.audioDuration||0);return {available:Boolean(audio||result?.audioAvailable),audio:audio||null,duration,valid:Boolean(audio)&&(!duration||Math.abs(duration-expectedDuration)<=Math.max(2,expectedDuration*.03)),provider:result?.provider||result?.generationModel||null};}
export function createMusicFallback({duration=30,bpm=112,energy=.72,seed='provider-fallback'}={}){const wav=createOriginalMusicWav(duration,bpm,{energy,seed});return {audio:wav,audioAvailable:true,source:'original-local-fallback',providerGrade:false,duration,provider:null};}
export function resolveProviderMusic(result,args={}){const checked=validateProviderAudio(result,args);return checked.valid?{...result,audioAvailable:true,source:'provider',providerGrade:true,duration:checked.duration||args.expectedDuration}:createMusicFallback(args);}
