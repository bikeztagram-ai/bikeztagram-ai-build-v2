export function validateCreativeStudioResult(result={}){const errors=[];if(!result.execution)errors.push('Missing execution plan.');if(!result.health)errors.push('Missing project health.');if(!result.renderJob)errors.push('Missing render job.');if(!result.campaign)errors.push('Missing campaign plan.');if(!result.run)errors.push('Missing pipeline run.');return {valid:errors.length===0,errors};}
