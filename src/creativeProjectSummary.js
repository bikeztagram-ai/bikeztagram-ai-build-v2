export function summarizeCreativeProject(project={}){const stages=project.pipeline?.stages||project.stages||[];const complete=stages.filter(s=>s.status==='complete'||s.status==='ready').length;return {id:project.id||null,title:project.title||null,stageCount:stages.length,readyCount:complete,progress:stages.length?Number((complete/stages.length).toFixed(2)):0,outputs:project.outputs?.length||0};}
