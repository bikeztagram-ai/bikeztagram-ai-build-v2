export function classifyBeatRevision(text=''){const t=String(text).toLowerCase();const targets=[];if(t.includes('beat')||t.includes('sync'))targets.push('beatAlignment');if(t.includes('drop'))targets.push('dropMoments');if(t.includes('faster'))targets.push('cutDensity','energy');if(t.includes('slower'))targets.push('breathingRoom','energy');if(t.includes('music'))targets.push('musicStructure');return {text:String(text),targets:targets.length?targets:['review']};}
