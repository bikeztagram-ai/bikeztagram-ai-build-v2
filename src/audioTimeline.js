/* BIKEZTAGRAM AI — audio timeline: beat-aligned edit/audio event scheduling. */
const n=(v,d=0)=>{const x=Number(v);return Number.isFinite(x)?x:d};
export function buildAudioTimeline({audioDirection={},cuts=[],mixPlan={}}={}){const beats=Array.isArray(audioDirection.beatMarkers)?audioDirection.beatMarkers:[],tol=n(audioDirection?.editSync?.snapTolerance,.14);const events=[];cuts.forEach((cut,index)=>{const raw=n(cut.startTime??cut.start,0),nearest=beats.reduce((b,t)=>Math.abs(t-raw)<Math.abs(b-raw)?t:b,beats[0]??raw);events.push({type:'cut',index,time:Math.abs(nearest-raw)<=tol?nearest:raw,sourceTime:raw,beatAligned:Math.abs(nearest-raw)<=tol,role:cut.editorialRole||cut.role||'variation'});});if(Number.isFinite(audioDirection.impactMarker))events.push({type:'impact',time:n(audioDirection.impactMarker,0)});events.sort((a,b)=>a.time-b.time);return{version:'audio-timeline-v1',events,mix:mixPlan,beatCount:beats.length,duration:n(audioDirection.duration,15)};}
