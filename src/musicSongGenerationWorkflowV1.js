export async function runSongGenerationWorkflow({brief,arrangement,compile,generate,score,revise}={}){const request=compile({brief,arrangement});let candidates=await generate(request);let ranked=await Promise.all((Array.isArray(candidates)?candidates:[candidates]).map(async output=>({output,evaluation:await score(output,request)})));ranked.sort((a,b)=>(Number(b.evaluation?.score)||0)-(Number(a.evaluation?.score)||0));const best=ranked[0]||null;if(best&&Number(best.evaluation?.score||0)<80&&revise){return revise({brief,request,best});}return {version:'music-song-generation-workflow-v1',request,candidates:ranked,best};}
