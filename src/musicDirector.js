export function planMusicDrivenEdit(project={}, music={}){const beats=music.beats||[];const duration=Number(project.duration||music.duration||30);const energy=music.energyCurve||[];return {duration,beats,energy,cutPolicy:music.style==='aggressive'?'beat-and-impact':'phrase-aware',sections:(music.sections||[]).map(s=>({start:s.start,end:s.end,role:s.role||'body',energy:s.energy??.5}))};}
