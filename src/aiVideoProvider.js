const sleep=ms=>new Promise(r=>setTimeout(r,ms));
export async function generateAIVideoScene({prompt,duration=5,ratio='720:1280',promptImage='',onProgress}={}){const start=await fetch('/api/video',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt,duration,ratio,promptImage:promptImage||undefined})});if(!start.ok){let data={};try{data=await start.json()}catch{}if(start.status===503)return null;throw Error(data.error||'AI video generation could not start.')}const task=await start.json();if(!task?.id)throw Error('Runway did not return a generation task id.');for(let attempt=0;attempt<30;attempt++){await sleep(attempt?5000:2000);const r=await fetch(`/api/video?id=${encodeURIComponent(task.id)}`);if(!r.ok)throw Error('Could not read AI video generation status.');const state=await r.json();if(state.status==='SUCCEEDED'){const url=Array.isArray(state.output)?state.output[0]:state.output;if(!url)throw Error('Runway completed without a video output.');onProgress?.(100);return{url,videoUrl:url,source:'runway-gen4.5',status:'ready',duration:state.duration||duration,mimeType:'video/mp4',provider:'Runway Gen-4.5'};}if(state.status==='FAILED'||state.status==='CANCELED')throw Error(`Runway video generation ${String(state.status).toLowerCase()}.`);onProgress?.(Math.min(95,Math.round((attempt+1)/30*95)));}throw Error('AI video generation timed out while waiting for Runway.');}
