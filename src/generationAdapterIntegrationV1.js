export async function executeCandidateGeneration({adapter,request,score=null}={}){if(!adapter?.generate)throw new Error('generation-adapter-required');const output=await adapter.generate(request);const quality=score?await score(output,request):null;return {status:'generated',modelId:adapter.id||null,output,quality};}
export async function executeCandidates({adapter,request,count=1,score=null}={}){const results=[];for(let i=0;i<Math.max(1,count);i++){results.push(await executeCandidateGeneration({adapter,request:{...request,candidateIndex:i},score}));}return results.sort((a,b)=>(Number(b.quality)||0)-(Number(a.quality)||0));}
