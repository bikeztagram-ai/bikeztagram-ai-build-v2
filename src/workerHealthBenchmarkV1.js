export async function benchmarkWorkerHealth({worker,timeoutMs=5000}={}){const started=Date.now();try{const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeoutMs);const r=await fetch(`${worker.baseUrl}/health`,{signal:controller.signal});clearTimeout(timer);return {workerId:worker.id,healthy:r.ok,latencyMs:Date.now()-started,status:r.status};}catch(error){return {workerId:worker?.id||null,healthy:false,latencyMs:Date.now()-started,error:error?.name||'worker-health-failed'};}}
