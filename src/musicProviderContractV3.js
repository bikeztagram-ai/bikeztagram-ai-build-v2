import {buildMusicGenerationPlan} from './musicCompositionV3.js';
import {validateProviderAudio,createMusicFallback} from './musicProviderAdapterV2.js';
export function buildProviderMusicRequest({duration=30,prompt='',genre='cinematic',mood='cinematic',energy=.72,bpm=112,filmType='trailer'}={}){const plan=buildMusicGenerationPlan({duration,prompt,genre,mood,energy,bpm,filmType});return {version:'music-provider-v3',request:{duration:plan.duration,genre:plan.genre,mood:plan.mood,energy:plan.energy,bpm,sections:plan.sections,production:plan.production,originality:plan.copyright},composition:plan,quality:{target:'professional-song',arrangement:true,motifDevelopment:true,sectionVariation:true,dynamics:true,mix:true,master:true},fallback:{allowed:true,clearlyLabeled:true,neverFinalMaster:true},copyright:{originalOnly:true,noNamedSongImitation:true,noExistingMelodyReproduction:true}};}
export function validateProviderMusicResponse(response,{duration=30}={}){const checked=validateProviderAudio(response,{expectedDuration:Number(duration)||30});return {...checked,requiresFallback:!checked.valid,professionalMaster:Boolean(checked.valid&&response?.providerGrade!==false)};}
export function resolveProfessionalMusic(response,args={}){const checked=validateProviderMusicResponse(response,args);if(checked.valid)return {accepted:true,professionalMaster:true,audio:checked.audio,duration:checked.duration,provider:checked.provider,source:'provider'};const fallback=createMusicFallback({...args,duration:Number(args.duration)||30});return {accepted:false,professionalMaster:false,source:'original-local-fallback',providerGrade:false,reason:'provider-master-unavailable',fallback};}
export function assertProfessionalMaster(result){if(!result?.accepted||!result?.professionalMaster||!result?.audio)throw new Error('Professional master required: validated provider audio is missing. Local fallback cannot be treated as final master.');return true;}
