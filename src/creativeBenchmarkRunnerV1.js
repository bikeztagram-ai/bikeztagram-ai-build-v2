/* Executes benchmark cases through an injected adapter and records evidence. */
export async function runCreativeBenchmark(cases=[],adapter,{observe=null}={}){const results=[];for(const test of cases){const started=Date.now();let output,error=null;try{output=await adapter.generate(test.input);}catch(e){error=String(e);}const evidence=typeof observe==='function'?await observe({test,output,error}):[];results.push({id:test.id,type:test.type,modelId:adapter.id,elapsedMs:Date.now()-started,output,error,evidence});}return {version:'creative-benchmark-run-v1',modelId:adapter.id,cases:results};}
