# Memory Loading Acceptance

A valid autonomous product batch must load `builder/quality/project-memory.md`, `builder/quality/lessons.md`, and `config/autonomous-builder-queue.json` before coding. The batch objective remains authoritative. Durable reusable lessons may be recorded back into project memory; secrets and transient chat details must never be stored.
