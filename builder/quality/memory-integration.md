# Persistent memory integration

The autonomous worker now explicitly instructs each coding pass to load project memory, quality lessons and queue context before editing. The memory is durable repository state; the current batch objective remains authoritative.