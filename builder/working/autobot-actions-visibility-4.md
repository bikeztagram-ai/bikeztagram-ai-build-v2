# AutoBot Actions

The proven parallel AutoBot fleet is intentionally manual-only.

Workflow: `.github/workflows/autobot-parallel-proven-fleet.yml`

Trigger: `workflow_dispatch` only. Do not add push or pull_request triggers.

Default first run: 15m with `qwen2.5-coder:7b`.
