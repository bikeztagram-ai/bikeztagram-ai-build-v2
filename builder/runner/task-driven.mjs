import { Sandbox } from '@vercel/sandbox';
import { mkdir, writeFile } from 'node:fs/promises';

const REPO_URL=process.env.BUILDER_REPO_URL||'https://github.com/bikeztagram-ai/bikeztagram-ai-build-v2.git';
const BATCH_ID=process.env.BUILDER_BATCH_ID||`batch-${Date.now()}`;
const BRANCH=process.env.BUILDER_WORKING_BRANCH||`autonomous-builder/${BATCH_ID}`;
const BASE_BRANCH=process.env.BUILDER_BASE_BRANCH||'main';
const RESUME_EXISTING=process.env.BUILDER_RESUME_EXISTING==='true';
const MAX_MINUTES=Math.min(Number(process.env.BUILDER_MAX_MINUTES||45),45);
const MAX_PASSES=Math.max(1,Math.min(Number(process.env.BUILDER_MAX_PASSES||2),3));
const ROOT='/vercel/sandbox';
const REPO_DIR=`${ROOT}/repo`;
const ASKPASS=`${ROOT}/git-askpass.sh`;
const PROTECTED=(process.env.BUILDER_PROTECTED_PATHS||'.github/workflows/,builder/runner/').split(',').map(x=>x.trim()).filter(Boolean);
const AGENT_CMD=process.env.BUILDER_AGENT_CMD?JSON.parse(process.env.BUILDER_AGENT_CMD):['npx','--yes','@openai/codex@0.150.0','exec','--full-auto'];
const AGENT_PROVIDER=process.env.BUILDER_AGENT_PROVIDER||'openai-codex';
const AGENT_MODEL=process.env.BUILDER_AGENT_MODEL||'gpt-5.6';
const OBJECTIVE=process.env.BUILDER_OBJECTIVE||'';
const ACCEPTANCE=(process.env.BUILDER_ACCEPTANCE||'').split(';').map(x=>x.trim()).filter(Boolean);
const VERIFY_COMMANDS=(process.env.BUILDER_VERIFY_COMMANDS||'npm run build').split(',').map(x=>x.trim()).filter(Boolean);
const COMMAND_TIMEOUT_MS=Math.max(60_000,Math.min(Number(process.env.BUILDER_COMMAND_TIMEOUT_MS||18*60*1000),MAX_MINUTES*60*1000));
const results=[];
const compact=(value,limit=12000)=>{const s=String(value||'').trim();return s.length<=limit?s:`${s.slice(0,limit)}\n...[truncated]`;};
const text=r=>`${r?.stdout||''}\n${r?.stderr||''}`.toLowerCase();
const providerFailure=r=>r?.exitCode!==0&&/model .*not found|invalid model|api key|authentication|unauthenticated|permission denied|forbidden|\b401\b|\b403\b|\b404\b|unknown argument|unknown option/.test(text(r));
const quotaFailure=r=>/quota|rate limit|too many requests|resource exhausted|\b429\b|usage limit|credit.*depleted|billing/.test(text(r));
const safeBranch=b=>b&&b!=='main'&&b!=='master'&&!b.startsWith('production/');
const shellQuote=value=>`'${String(value).replace(/'/g,"'\\''")}'`;
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));

async function run(sandbox,cmd,args=[],cwd=REPO_DIR,env){const r=await sandbox.runCommand({cmd,args,cwd,...(env?{env}:{}),signal:AbortSignal.timeout(COMMAND_TIMEOUT_MS)});const out={command:[cmd,...args].join(' '),exitCode:r.exitCode,stdout:compact(await r.stdout()),stderr:compact(await r.stderr())};results.push(out);return out;}
async function runAgent(sandbox,promptText,env,pass){const stamp=`${BATCH_ID}-${pass}-${Date.now()}`,logPath=`/tmp/autobot-agent-${stamp}.log`,exitPath=`/tmp/autobot-agent-${stamp}.exit`;const args=[...AGENT_CMD.slice(1),promptText];const commandLine=[AGENT_CMD[0],...args].map(shellQuote).join(' ');const launchScript=`rm -f ${shellQuote(logPath)} ${shellQuote(exitPath)}; nohup sh -lc ${shellQuote(`${commandLine} > ${shellQuote(logPath)} 2>&1; code=$?; printf '%s' \"$code\" > ${shellQuote(exitPath)}`)} >/dev/null 2>&1 & echo $!`;const launch=await run(sandbox,'sh',['-lc',launchScript]);if(launch.exitCode)return launch;const started=Date.now();let last='';while(Date.now()-started<COMMAND_TIMEOUT_MS){const probe=await run(sandbox,'sh',['-lc',`if [ -f ${shellQuote(exitPath)} ]; then cat ${shellQuote(exitPath)}; else printf RUNNING; fi`]);if(probe.exitCode===0&&probe.stdout.trim()!=='RUNNING'){const log=await run(sandbox,'cat',[logPath]);const code=Number.parseInt(probe.stdout.trim(),10);return{command:commandLine,exitCode:Number.isFinite(code)?code:1,stdout:compact(log.stdout),stderr:compact(log.stderr)};}const tail=await run(sandbox,'tail',['-n','12',logPath]);if(tail.exitCode===0&&tail.stdout.trim()&&tail.stdout.trim()!==last){last=tail.stdout.trim();console.log(`[AutoBot] Codex update — ${BATCH_ID}, pass ${pass}:\n${compact(last,4000)}`);}await sleep(5000);}return{command:commandLine,exitCode:124,stdout:last,stderr:`Agent exceeded command timeout of ${COMMAND_TIMEOUT_MS}ms.`};}
async function verify(sandbox){const failures=[];const diff=await run(sandbox,'git',['diff','--check']);if(diff.exitCode)failures.push(`git diff --check failed: ${compact(diff.stderr||diff.stdout)}`);for(const spec of VERIFY_COMMANDS){const [cmd,...args]=spec.split(/\s+/);const r=await run(sandbox,cmd,args);if(r.exitCode)failures.push(`${spec} failed (exit ${r.exitCode}).\n${compact(r.stderr||r.stdout)}`);}return failures;}
async function protect(sandbox){const status=await run(sandbox,'git',['status','--short']);if(status.exitCode)throw Error(`git status failed: ${status.stderr}`);const changed=status.stdout.split('\n').filter(Boolean);const blocked=changed.map(x=>x.replace(/^[MADRCU?!]{1,2}\s+/,'').trim()).filter(p=>PROTECTED.some(prefix=>p===prefix||p.startsWith(prefix)));for(const prefix of PROTECTED){await run(sandbox,'git',['restore','--source=HEAD','--staged','--worktree','--',prefix]);await run(sandbox,'git',['clean','-fd','--',prefix]);}return blocked;}
async function publish(sandbox,gitEnv){await run(sandbox,'git',['config','user.name','Bikeztagram Autonomous Builder']);await run(sandbox,'git',['config','user.email','41898282+github-actions[bot]@users.noreply.github.com']);const add=await run(sandbox,'git',['add','-A']);if(add.exitCode)throw Error(`git add failed: ${add.stderr}`);const commit=await run(sandbox,'git',['commit','-m',`builder: complete ${BATCH_ID}`]);if(commit.exitCode)throw Error(`git commit failed: ${commit.stderr}`);const sha=(await run(sandbox,'git',['rev-parse','HEAD'])).stdout.trim();const fetch=await run(sandbox,'git',['fetch','origin',`${BRANCH}:refs/remotes/origin/${BRANCH}`],REPO_DIR,gitEnv);const args=fetch.exitCode===0?['push','--force-with-lease=refs/heads/'+BRANCH+':refs/remotes/origin/'+BRANCH,'--set-upstream','origin',`HEAD:${BRANCH}`]:['push','--set-upstream','origin',`HEAD:refs/heads/${BRANCH}`];const push=await run(sandbox,'git',args,REPO_DIR,gitEnv);if(push.exitCode)throw Error(`git push failed: ${push.stderr}`);return sha;}
function prompt(pass,failures){return[`BIKEZTAGRAM AUTONOMOUS ENGINEERING WORKER — ${BATCH_ID}`,'Execute ONLY the pre-approved engineering task. Do not invent unrelated roadmap work.','Read builder/quality/project-memory.md, builder/quality/lessons.md and config/autonomous-builder-queue.json before editing.','OBJECTIVE:',OBJECTIVE,'ACCEPTANCE:',...ACCEPTANCE.map(x=>`- ${x}`),`PASS ${pass}/${MAX_PASSES}`,failures.length?`PREVIOUS VERIFICATION FAILURES:\n${failures.join('\n\n')}`:'','Work as a production engineer. Make a complete substantive implementation, integrate it with existing runtime contracts, run relevant tests, repair failures, and stop only when acceptance is genuinely satisfied.','Never use or introduce Gemini, Google GenAI, Gemini CLI, Gemini API keys, or Gemini model identifiers. Use the configured OpenAI Codex worker only.','Do not modify .github/workflows/**, builder/runner/**, the durable queue, or production deployment. Do not commit or push; the runner owns Git.','Avoid documentation-only work, speculative refactors and fake/procedural substitutes for real AI capabilities.'].filter(Boolean).join('\n');}

async function main(){if(!safeBranch(BRANCH))throw Error(`Refusing unsafe branch: ${BRANCH}`);if(!OBJECTIVE)throw Error('BUILDER_OBJECTIVE is required');if(!process.env.GITHUB_TOKEN)throw Error('GITHUB_TOKEN is required');if(!process.env.OPENAI_API_KEY)throw Error('OPENAI_API_KEY is required for the OpenAI Codex worker');if(!process.env.VERCEL_TOKEN||!process.env.VERCEL_TEAM_ID||!process.env.VERCEL_PROJECT_ID)throw Error('Vercel sandbox credentials are required');const gitEnv={GITHUB_TOKEN:process.env.GITHUB_TOKEN,GIT_ASKPASS:ASKPASS,GIT_TERMINAL_PROMPT:'0'},agentEnv={OPENAI_API_KEY:process.env.OPENAI_API_KEY};let sandbox,status='FAILED',failure=null,commitSha=null,passes=0,failures=[],quotaDetected=false,providerFailed=false,noChanges=false,protectedChangesReset=[];const startedAt=new Date().toISOString();try{const sandboxTimeoutMs=MAX_MINUTES*60*1000;sandbox=await Sandbox.create({teamId:process.env.VERCEL_TEAM_ID,projectId:process.env.VERCEL_PROJECT_ID,token:process.env.VERCEL_TOKEN,runtime:'node24',resources:{vcpus:4},persistent:false,timeout:sandboxTimeoutMs,networkPolicy:'allow-all'});const askpass=await run(sandbox,'sh',['-lc',`cat > '${ASKPASS}' <<'EOF'\n#!/bin/sh\ncase "$1" in *Username*) printf '%s\\n' 'x-access-token';; *) printf '%s\\n' "$GITHUB_TOKEN";; esac\nEOF\nchmod 700 '${ASKPASS}'`],ROOT,gitEnv);if(askpass.exitCode)throw Error(`Git auth helper setup failed: ${askpass.stderr}`);const clone=await run(sandbox,'git',['clone','--branch',BASE_BRANCH,'--depth','1',REPO_URL,REPO_DIR],ROOT,gitEnv);if(clone.exitCode)throw Error(`Clone failed: ${clone.stderr}`);if(RESUME_EXISTING){const fetch=await run(sandbox,'git',['fetch','origin',`${BRANCH}:refs/remotes/origin/${BRANCH}`],REPO_DIR,gitEnv);if(fetch.exitCode)throw Error(`Could not fetch existing branch ${BRANCH}: ${fetch.stderr}`);}const branch=RESUME_EXISTING?await run(sandbox,'git',['checkout','-B',BRANCH,`origin/${BRANCH}`]):await run(sandbox,'git',['checkout','-b',BRANCH]);if(branch.exitCode)throw Error(`Branch checkout failed: ${branch.stderr}`);const install=await run(sandbox,'npm',['install','--no-audit','--no-fund','--no-package-lock']);if(install.exitCode)throw Error(`Dependency install failed: ${install.stderr||install.stdout}`);for(passes=1;passes<=MAX_PASSES;passes++){const agent=await runAgent(sandbox,prompt(passes,failures),agentEnv,passes);if(quotaFailure(agent)){quotaDetected=true;failures=[`OpenAI provider quota/rate limit detected.\n${compact(agent.stderr||agent.stdout)}`];break;}if(providerFailure(agent)){providerFailed=true;failures=[`OpenAI Codex provider/model failure (exit ${agent.exitCode}).\n${compact(agent.stderr||agent.stdout)}`];break;}if(agent.exitCode){failures=[`Codex agent failed (exit ${agent.exitCode}).\n${compact(agent.stderr||agent.stdout)}`];break;}const changed=await run(sandbox,'git',['status','--short']);if(changed.exitCode)throw Error(`git status failed: ${changed.stderr}`);if(!changed.stdout.trim()){noChanges=true;failures=['Agent completed without producing repository changes.'];break;}protectedChangesReset=await protect(sandbox);failures=await verify(sandbox);if(!failures.length){status='VERIFIED';break;}}if(status==='VERIFIED')commitSha=await publish(sandbox,gitEnv);else if(!failure)failure=providerFailed?'OpenAI Codex provider/model failed before reliable execution.':quotaDetected?'OpenAI quota/rate limit detected.':noChanges?'Builder produced no repository changes.':'Builder did not reach a verified state.';}catch(error){failure=error?.message||String(error);}finally{if(sandbox)await sandbox.stop().catch(()=>{});}const report={batchId:BATCH_ID,status,provider:AGENT_PROVIDER,model:AGENT_MODEL,startedAt,finishedAt:new Date().toISOString(),passes,commitSha,failure,quotaDetected,providerFailure:providerFailed,noChangesProduced:noChanges,protectedChangesReset,maxPasses:MAX_PASSES,commandTimeoutMs:COMMAND_TIMEOUT_MS,sandboxTimeoutMs:MAX_MINUTES*60*1000,failures,commands:results};await mkdir('/tmp/builder-report',{recursive:true});await writeFile(`/tmp/builder-report/${BATCH_ID}.json`,JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));if(status!=='VERIFIED')process.exitCode=1;}
main().catch(error=>{console.error(error);process.exitCode=1;});
