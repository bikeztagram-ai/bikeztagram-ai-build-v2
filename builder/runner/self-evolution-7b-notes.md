# 7B self-evolution runtime

Qwen 2.5 Coder 7B is the primary substantive self-evolution model. The runtime deliberately gives it a long inference budget because the observed CPU-only Aider run consumed almost the entire 15-minute smoke window before timing out. Longer runs should favour complete verified objectives over rapid retries.
