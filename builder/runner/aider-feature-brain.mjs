#!/usr/bin/env node
/** Bikeztagram AutoBot — Aider-backed feature engineer. */
import fs from 'node:fs';
import path from 'node:path';
import { execFileSync, spawnSync } from 'node:child_process';
import { loadAiderState, saveAiderState } from './aider-state-store.mjs';

const root=process.cwd();
const run=(cmd,args,options={})=>spawnSync(cmd,args,{cwd:root,encoding:'utf8',stdio:'inherit',...options});
const protocol=process.env.AUTOBOT_FEATURE_PROTOCOL||'aider-diff-v5';
const maxPasses=Math.max(1,Math.min(3,Number(process.env.AUTOBOT_FEATURE_PASSES||2)));
const model=process.env.AUTOBOT_AIDER_MODEL||process.env.LOCAL_AI_MODEL||'ollama_chat/qwen2.5-coder:7b';
const requestedMinutes=Math.max(1,Number.parseInt(process.env.BUILDER_MAX_MINUTES||'15',10));
const configuredDeadline=Number.parseInt(process.env.AUTOBOT_FEATURE_DEADLINE_EPOCH_MS||'',10);
const deadline=Number.isFinite(configuredDeadline)&&configuredDeadline>Date.now()?configuredDeadline:Date.now()+requestedMinutes*60_000;
const normalDeadline=Number.parseInt(process.env.AUTOBOT_FEATURE_NORMAL_DEADLINE_EPOCH_MS||String(deadline),10);
const perCallMaxMs=Math.max(30_000,Number.parseInt(process.env.AUTOBOT_AIDER_CALL_TIMEOUT_MS||String(6*60*60*1000),10));
const specialist=process.env.AUTOBOT_SPECIALIST_MODE==='true';
const specialistEditFormat=String(process.env.AUTOBOT_SPECIALIST_AIDER_EDIT_FORMAT||'udiff').trim().toLowerCase();
const specialistMapTokens=Math.max(512,Math.min(4096,Number.parseInt(process.env.AUTOBOT_SPECIALIST_MAP_TOKENS||'768',10)||768));
if(specialist&&!['diff','udiff','whole'].includes(specialistEditFormat))throw new Error(`Unsupported specialist Aider edit format: ${specialistEditFormat}`);
const statePath=path.join(root,'builder/working/aider-feature-brain-state.json');
const directivePath=path.join(root,'builder/brain/autobot-product-directive.md');
const assignmentPath=process.env.AUTOBOT_ORCHESTRATOR_ASSIGNMENT_PATH||path.join(root,'builder/working/autobot-orchestrator-assignment.json');
let directive='';try{directive=fs.readFileSync(directivePath,'utf8').trim();}catch{}
function remainingMs(){return Math.max(0,deadline-Date.now());}
function normalRemainingMs(){return Math.max(0,normalDeadline-Date.now());}
function loadObjectives(){const p=path.join(root,'builder/brain/feature-objectives.json');try{return JSON.parse(fs.readFileSync(p,'utf8')).objectives||[];}catch(e){try{const changed=execFileSync('git',['status','--short','--',p],{cwd:root,encoding:'utf8'}).trim();if(changed){execFileSync('git',['restore','--','builder/brain/feature-objectives.json'],{cwd:root});return JSON.parse(fs.readFileSync(p,'utf8')).objectives||[];}}catch{}throw new Error(`feature objectives JSON is invalid: ${e.message}`);}}
const objectives=loadObjectives();
const loaded=loadAiderState(statePath,{protocol,completed:[],failed:[],runs:0,inProgress:null});
const state=loaded.state;
function assignment(){if(!specialist)return null;try{return JSON.parse(fs.readFileSync(assignmentPath,'utf8'));}catch(e){throw new Error(`orchestrator assignment unavailable: ${e.message}`);}}
const assigned=assignment();
function objective(){if(assigned?.objective?.title){const o={...assigned.objective,id:assigned.objective.id||`orchestrated-${Date.now()}`,enabled:true,dependsOn:[],files:Array.isArray(assigned.objective.files)?assigned.objective.files:[],acceptance:Array.isArray(assigned.objective.acceptance)?assigned.objective.acceptance:[],constraints:Array.isArray(assigned.objective.constraints)?assigned.objective.constraints:[]};if(!o.files.length)throw new Error('orchestrator assignment has no scoped files');return o;}const done=new Set(state.completed||[]);const resume=state.inProgress?.id;if(resume){const o=objectives.find(x=>x?.id===resume&&x.enabled!==false&&!done.has(x.id));if(o)return o;}return objectives.find(x=>x?.enabled!==false&&!done.has(x.id)&&(!x.dependsOn||x.dependsOn.every(d=>done.has(d))))||null;}
function filesFor(o){return Array.isArray(o?.files)?o.files.filter(Boolean):[];}
function promptFor(o,pass){const files=filesFor(o);const review=pass>1;const targetMap=Array.isArray(assigned?.targetMap)?assigned.targetMap:[];const learningHint=assigned?.learning?.promptHint||'';const targetContext=targetMap.length?`TARGET MAP (exact source anchors; use these to avoid broad repository scanning): ${JSON.stringify(targetMap)}`:'TARGET MAP MISSING: stop and report the targeting contract failure rather than guessing.';const editInstruction=specialist?`Use Aider as the editor: directly modify the supplied objective files now. Do NOT merely explain the change, return a hypothetical diff, produce prose instead of edits, or wait for another agent. The Aider CLI will apply the requested ${effectiveEditFormat} edit format itself. The Aider chat filenames are the basename paths supplied to Aider (for example director.js), not the repository src/... path; use the exact chat filename when constructing edits. For dense/minified files, whole-file editing is intentional because line-oriented hunks are unreliable. Make real, minimal changes that improve the production behaviour and satisfy the acceptance criteria.`:'Actually edit the supplied files; do not merely describe the change.';return ['You are the Bikeztagram AI autonomous feature engineer.','Objective: '+(o.title||o.id),`Pass ${pass} of ${maxPasses}.`,review?'Review the existing implementation critically and fix only justified weaknesses.':'Make one coherent product-quality improvement.',`Acceptance criteria: ${JSON.stringify(o.acceptance||[])}`,`Objective-scoped product files: ${files.join(', ')}`,`Objective constraints: ${JSON.stringify(o.constraints||[])}`,directive?`PRODUCT QUALITY DIRECTIVE:\n${directive}`:'Apply the project quality principles from the acceptance criteria.',targetContext,'Inspect the supplied product file and use the exact target-map context as the starting point. Do not spend the edit budget scanning the repository for a target that has already been identified.','ONLY modify the supplied objective files. Do not modify workflows, builder infrastructure, dependencies, secrets, or unrelated paths.','Preserve public contracts, safety, rollback, audit, production verification and Gemini-free rules.','Do not introduce arbitrary limits that discard useful user media.','Do not add helpers, metadata or scores unless they are connected to and consumed by production behaviour.','Make the smallest justified patch. If an edit anchor does not exactly match the current file, re-read the current target context and adapt the patch to the actual code; never invent surrounding code or abandon the edit because a guessed anchor is stale.',learningHint?`LEARNED EDITING HINT: ${learningHint}`:'',editInstruction,'Do not merge or create a pull request.'].filter(Boolean).join('\n');}
function tracked(){try{return execFileSync('git',['status','--short'],{cwd:root,encoding:'utf8'}).split(/\r?\n/).filter(Boolean).map(x=>x.slice(3).trim()).filter(Boolean);}catch{return[];}}
function snapshot(files){const p=path.join(root,'builder/working',`aider-pass-snapshot-${process.pid}.patch`);fs.mkdirSync(path.dirname(p),{recursive:true});try{fs.writeFileSync(p,execFileSync('git',['diff','--binary','--',...files],{cwd:root,encoding:'utf8'}));}catch{}return p;}
function discard(p){try{fs.unlinkSync(p);}catch{}}
function restore(o,p){for(const f of filesFor(o)){try{execFileSync('git',['restore','--worktree','--',f],{cwd:root});}catch{}try{execFileSync('git',['clean','-fd','--',f],{cwd:root});}catch{}}try{if(fs.existsSync(p)&&fs.statSync(p).size)execFileSync('git',['apply','--whitespace=nowarn',p],{cwd:root});}catch(e){console.error(`[aider] snapshot restore failed: ${e.message}`);}discard(p);}
function scope(before,o){const allowed=new Set(filesFor(o));const after=tracked();const bad=after.filter(x=>!allowed.has(x)&&!before.has(x));if(bad.length)throw new Error(`Aider modified files outside objective scope: ${bad.join(', ')}`);}
function verify(){execFileSync('git',['diff','--check'],{cwd:root,stdio:'inherit'});const r=remainingMs();if(r<35_000)throw new Error('insufficient remaining run budget for build verification');const b=run('npm',['run','build'],{timeout:Math.min(120_000,r-5_000)});if(b.error||b.status!==0)throw new Error(`npm run build failed with status ${b.status??'error'}`);const q=remainingMs();if(q<35_000)throw new Error('insufficient remaining run budget for product quality verification');const v=run('npm',['run','verify:autobot-product-change-quality'],{timeout:Math.min(120_000,q-5_000)});if(v.error||v.status!==0)throw new Error(`product quality verification failed with status ${v.status??'error'}`);}
function hasChanges(o){const allowed=new Set(filesFor(o));return tracked().some(f=>allowed.has(f));}
const o=objective();
if(!o){console.log(JSON.stringify({ok:true,protocol,status:'no-eligible-objective'}));process.exit(0);}
const files=filesFor(o);if(!files.length){console.error(`[aider] objective ${o.id} has no scoped files`);process.exit(1);}
const srcOnly=files.every(f=>f.startsWith('src/'));const cwd=srcOnly?path.join(root,'src'):root;const aiderFiles=srcOnly?files.map(f=>f.slice(4)):files;function chooseSpecialistEditFormat(){if(!specialist)return 'whole';for(const file of files){try{const text=fs.readFileSync(path.join(root,file),'utf8');const maxLine=Math.max(...text.split(/\r?\n/).map(line=>line.length),0);if(maxLine>1200)return 'whole';}catch{}}return specialistEditFormat;}const effectiveEditFormat=chooseSpecialistEditFormat();const prior=state.inProgress?.id===o.id?Math.max(0,Number(state.inProgress.completedPasses||0)):0;const first=Math.min(maxPasses,prior+1);let success=false;
for(let pass=first;pass<=maxPasses;pass++){
  const rem=remainingMs();if(rem<35_000||normalRemainingMs()<35_000&&pass>first)break;
  state.runs=(state.runs||0)+1;const before=new Set(tracked());const snap=snapshot(files);const passesRemaining=Math.max(1,maxPasses-pass+1);
  const timeout=Math.min(perCallMaxMs,Math.max(30_000,Math.floor(rem/passesRemaining)-5_000));
  const specialistMapArg=specialist?`--map-tokens=2048`:`--map-tokens=768`;
  const args=[`--model=${model}`,`--timeout=${Math.max(30,Math.floor(timeout/1000))}`,'--yes-always','--no-auto-commits','--no-dirty-commits','--no-gitignore','--no-show-model-warnings',...(specialist?['--no-git','--map-tokens=2048','--subtree-only',`--edit-format=${effectiveEditFormat}`]:[specialistMapArg,'--subtree-only','--edit-format=whole']),'--message',promptFor(o,pass),...aiderFiles];
  const result=spawnSync('aider',args,{cwd,encoding:'utf8',stdio:'inherit',timeout});
  const changed=hasChanges(o);
  if(result.error||result.status!==0){
    if(specialist&&changed){try{scope(before,o);verify();state.inProgress={id:o.id,completedPasses:pass,lastVerifiedAt:new Date().toISOString(),remainingPasses:Math.max(0,maxPasses-pass),aiderLifecycle:'nonzero-but-verified'};state.failed=[...(state.failed||[]),{id:o.id,pass,code:result.error?.code||result.status||'nonzero',preservedSpecialistChanges:true,verifiedAfterNonzero:true}];saveAiderState(statePath,state);discard(snap);console.warn('[aider] specialist candidate survived non-zero Aider exit and passed independent gates; preserving for downstream QA/Reviewer.');success=pass>=maxPasses;if(success)break;continue;}catch(e){console.warn(`[aider] preserved candidate failed verification: ${e.message}`);}}
    if(specialist)discard(snap);else restore(o,snap);state.failed=[...(state.failed||[]),{id:o.id,pass,code:result.error?.code||result.status||'process-error',preservedSpecialistChanges:specialist&&changed}];saveAiderState(statePath,state);if(remainingMs()<35_000)break;continue;
  }
  try{scope(before,o);if(!changed)throw new Error('Aider completed without materializing a scoped product change');verify();state.inProgress={id:o.id,completedPasses:pass,lastVerifiedAt:new Date().toISOString(),remainingPasses:Math.max(0,maxPasses-pass)};saveAiderState(statePath,state);discard(snap);success=pass>=maxPasses;if(success)break;}catch(e){if(specialist)discard(snap);else restore(o,snap);state.failed=[...(state.failed||[]),{id:o.id,pass,code:'verification',error:e.message,preservedSpecialistChanges:specialist&&changed}];saveAiderState(statePath,state);if(remainingMs()<35_000)break;if(specialist)break;}
}
if(success){state.completed=[...(state.completed||[]),o.id];state.lastSuccess={id:o.id,at:new Date().toISOString()};state.inProgress=null;}else if(state.inProgress?.id===o.id){state.inProgress={...state.inProgress,remainingPasses:Math.max(0,maxPasses-Number(state.inProgress.completedPasses||0)),checkpointedAt:new Date().toISOString()};}
state.protocol=protocol;state.lastRunAt=new Date().toISOString();saveAiderState(statePath,state);console.log(JSON.stringify({ok:success,protocol,objective:o.id,objectiveSource:assigned?'orchestrator-assignment':'feature-library',assignedSpecialist:assigned?.specialist?.id||null,passes:maxPasses,startingPass:first,model,elapsedMs:(requestedMinutes*60_000)-remainingMs(),remainingMs:remainingMs(),normalRemainingMs:normalRemainingMs(),resumable:!success&&state.inProgress?.id===o.id,preservedSpecialistChanges:specialist&&hasChanges(o),adversarialReviewEnabled:maxPasses>1,productDirectiveLoaded:Boolean(directive),postChangeProductQualityGuard:true,editFormat:specialist?effectiveEditFormat:'whole',executionMode:specialist?'direct-editor':'standard'}));process.exit(success?0:1);
