# AutoBot learning loop

AutoBot records outcomes, classifies recurring failures, proposes bounded improvements, and requires regression evidence before a lesson can become active.

The learning system is deliberately **not self-authorising**: it cannot change workflow security, merge/deploy behaviour, secrets, protected infrastructure, or its own safety policy without human review.

A lesson is useful only when a repeatable verification rule demonstrates the intended behavioural improvement.
