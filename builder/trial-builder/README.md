# Forge Trial Builder

This directory is the isolated experimental bot created from the Forge research lane.

The other nine research lanes provide evidence. The trial builder reads that evidence, inspects its own current implementation, performs one bounded self-improvement experiment, verifies it, and records the result.

The persistent branch is `autobot/trial-builder`. It is never merged automatically into `main`.
