import { get } from '@vercel/blob';
const text=(v)=>String(v??'').trim();
export function pathnameFromBlobUrl(value){const url=text(value);if(!url)return '';try{return decodeURIComponent(new URL(url).pathname.replace(/^\/+/,''));}catch{return '';}}
async function readHttpSource(url,label){const response=await fetch(url,{redirect:'follow'});if(!response.ok)throw new Error(`${label} public Blob URL returned HTTP ${response.status}.`);const contentType=text(response.headers.get('content-type'))||'application/octet-stream';const bytes=Buffer.from(await response.arrayBuffer());if(!bytes.length)throw new Error(`Vercel Blob returned an empty ${label}.`);return{bytes,contentType};}
async function readPublicSource(pathname,token,label){if(!token)throw new Error('PUBLIC_BLOB_READ_WRITE_TOKEN is missing.');const result=await get(pathname,{access:'public',token,useCache:false});if(!result||result.statusCode!==200||!result.stream)throw new Error(`${label} public SDK read returned no stream.`);const contentType=text(result.blob?.contentType)||'application/octet-stream';const bytes=Buffer.from(await new Response(result.stream).arrayBuffer());if(!bytes.length)throw new Error(`Vercel Blob returned an empty ${label}.`);return{bytes,contentType,blob:result.blob};}
async function readViaHead(url,label){const response=await fetch(url,{method:'HEAD',redirect:'follow'});return{ok:response.ok,status:response.status,contentType:text(response.headers.get('content-type'))};}
export async function readPrivateBlob({url='',pathname='',label='source media'}={}){const resolvedUrl=text(url),resolvedPathname=text(pathname)||pathnameFromBlobUrl(resolvedUrl),token=text(process.env.PUBLIC_BLOB_READ_WRITE_TOKEN),failures=[];if(resolvedUrl){try{return{...(await readHttpSource(resolvedUrl,label)),pathname:resolvedPathname||pathnameFromBlobUrl(resolvedUrl),blob:null,readMethod:'url'};}catch(error){failures.push(`url:${error?.message||error}`);try{const head=await readViaHead(resolvedUrl,label);failures.push(`head:${head.status}${head.contentType?` ${head.contentType}`:''}`);}catch(error2){failures.push(`head:${error2?.message||error2}`);}}}if(!resolvedPathname)throw new Error(`${label} has no Blob pathname or readable source URL. ${failures.join(' | ')}`);try{return{...(await readPublicSource(resolvedPathname,token,label)),pathname:resolvedPathname,readMethod:'public-sdk'};}catch(error){failures.push(`public-sdk:${error?.message||error}`);}throw new Error(`Canonical public Vercel Blob could not read ${label}. Verify the stored pathname belongs to this Blob store and that the server token matches it. ${failures.join(' | ')}`);}
