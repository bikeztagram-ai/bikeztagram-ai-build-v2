name: 🧩 AutoBot Persistent Specialists

on:
  workflow_dispatch:
    inputs:
      duration:
        description: Total AutoBot run budget (cycles continue inside this run)
        required: true
        default: 15m
        type: choice
        options: ['15m','30m','1h','4h','5h','5h30']
      local_model:
        description: Local discovery/coding model
        required: false
        default: qwen2.5-coder:7b
        type: string

permissions:
  contents: write
  pull-requests: write
  actions: read

concurrency:
  group: autobot-persistent-specialists
  cancel-in-progress: false

env:
  LOCAL_AI_MODEL: ${{ inputs.local_model }}
  AUTOBOT_DISCOVERY_MODEL: qwen2.5-coder:3b
  AUTOBOT_DISCOVERY_TIMEOUT_MS: 180000
  AUTOBOT_PARALLEL_BOTS: director-builder,timeline-builder,music-builder,scene-builder,rhythm-builder,render-builder,media-intelligence-builder,caption-builder,intent-builder,continuity-builder
  AUTOBOT_TOTAL_DURATION: ${{ inputs.duration }}
  AUTOBOT_CYCLE_MINUTES: ${{ inputs.duration == '15m' && '10' || inputs.duration == '30m' && '15' || '30' }}
  AUTOBOT_FINISH_GRACE_MINUTES: ${{ (inputs.duration == '1h' || inputs.duration == '4h' || inputs.duration == '5h' || inputs.duration == '5h30') && '15' || '0' }}
  AUTOBOT_CYCLE_SAFETY_MINUTES: '1'
  AUTOBOT_SPECIALIST_FALLBACK_MODEL: qwen2.5-coder:3b
  AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
  AUTOBOT_REPAIR_TIMEOUT_MS: '480000'
  AUTOBOT_MAX_NO_PROGRESS_CYCLES: '2'

jobs:
  # ISOLATED PRODUCTION FAN-OUT — all ten product specialists run as separate GitHub jobs/runners. Each owns one disjoint product file, its own worktree, Aider process and local AI brain. A failure is contained to that specialist; all results converge through the canonical fan-in ledger.
  # DIRECTOR — fully isolated production specialist. Its runner/worktree/Aider
  # stack is independent of every other specialist.
  experimental-director:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Director lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock
      - name: Install Director lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat
      - name: Start isolated Director AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m
      - name: Run Director specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: director-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: whole-src-single
          AUTOBOT_SPECIALIST_OBJECTIVE: "Production Director lane: improve hook-to-payoff shot selection or creative-intent weighting in src/director.js with one minimal, production-safe change. Preserve existing APIs and contracts. Acceptance:
- shot selection responds to creative intent while preserving evidence scoring and diversity
- only the owned file is changed
- syntax, build and product-quality verification pass
- leave a candidate when valid."
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs
      - name: Publish Director candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"
      - name: Independent QA + Reviewer Director candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/director-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/director-builder/autobot-candidate-review-director-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/director-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/director-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/director-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs director-builder
      - name: Upload Director worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-director-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/director-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/director-builder/autobot-candidate-review-director-builder.json
          if-no-files-found: ignore

  # TIMELINE — fully isolated production specialist. Its runner/worktree/Aider
  # stack is independent of every other specialist.
  experimental-timeline:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Timeline lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock
      - name: Install Timeline lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat
      - name: Start isolated Timeline AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m
      - name: Run Timeline specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: timeline-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: whole-root-single
          AUTOBOT_SPECIALIST_OBJECTIVE: "Production Timeline lane: improve source-aware timing, role-aware motion or transition cadence in src/executableTimeline.js with one minimal, production-safe change. Preserve existing APIs and contracts. Acceptance:
- timeline execution preserves source identity and editorial roles while improving motion or timing
- only the owned file is changed
- syntax, build and product-quality verification pass
- leave a candidate when valid."
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs
      - name: Publish Timeline candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"
      - name: Independent QA + Reviewer Timeline candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/timeline-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/timeline-builder/autobot-candidate-review-timeline-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/timeline-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/timeline-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/timeline-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs timeline-builder
      - name: Upload Timeline worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-timeline-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/timeline-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/timeline-builder/autobot-candidate-review-timeline-builder.json
          if-no-files-found: ignore

  experimental-music:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Music lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock

      - name: Install Music lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat

      - name: Start isolated Music AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m

      - name: Run Music specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: music-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: whole-src-single
          AUTOBOT_SPECIALIST_OBJECTIVE: "Experimental Music lane: improve prompt-responsive music energy in src/musicDirector.js with one minimal, production-safe change. Preserve existing APIs and contracts. Acceptance:
- energy responds to driving/fast/action creative intent
- only the owned file is changed
- syntax, build and product-quality verification pass
- leave a candidate when valid."
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs

      - name: Publish Music candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"

      - name: Independent QA + Reviewer Music candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/music-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/music-builder/autobot-candidate-review-music-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/music-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/music-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/music-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs music-builder
      - name: Upload Music worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-music-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/music-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/music-builder/autobot-candidate-review-music-builder.json
          if-no-files-found: ignore

  experimental-scene:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Scene lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock

      - name: Install Scene lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat

      - name: Start isolated Scene AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m

      - name: Run Scene specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: scene-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: whole-src-single
          AUTOBOT_SPECIALIST_OBJECTIVE: "Experimental Scene lane: improve duration-bounded scene shot continuity in src/universalCreativeSceneEngine.js with one minimal, production-safe change. Preserve existing APIs and contracts. Acceptance:
- scene shot count remains bounded and scales with duration
- only the owned file is changed
- syntax, build and product-quality verification pass
- leave a candidate when valid."
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs

      - name: Publish Scene candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"

      - name: Independent QA + Reviewer Scene candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/scene-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/scene-builder/autobot-candidate-review-scene-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/scene-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/scene-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/scene-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs scene-builder
      - name: Upload Scene worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-scene-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/scene-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/scene-builder/autobot-candidate-review-scene-builder.json
          if-no-files-found: ignore

  experimental-rhythm:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Rhythm lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock

      - name: Install Rhythm lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat

      - name: Start isolated Rhythm AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m

      - name: Run Rhythm specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: rhythm-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: whole-src-single
          AUTOBOT_SPECIALIST_OBJECTIVE: "Production Rhythm lane: improve beat-aware editorial pacing and duration distribution in src/editorialRhythm.js with one minimal, production-safe change. Preserve existing APIs and contracts. Acceptance:
- pacing responds to creative intent without exceeding duration bounds
- only the owned file is changed
- syntax, build and product-quality verification pass
- leave a candidate when valid."
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs

      - name: Publish Rhythm candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"

      - name: Independent QA + Reviewer Rhythm candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/rhythm-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/rhythm-builder/autobot-candidate-review-rhythm-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/rhythm-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/rhythm-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/rhythm-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs rhythm-builder
      - name: Upload Rhythm worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-rhythm-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/rhythm-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/rhythm-builder/autobot-candidate-review-rhythm-builder.json
          if-no-files-found: ignore

  experimental-render:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Render lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock

      - name: Install Render lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat

      - name: Start isolated Render AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m

      - name: Run Render specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: render-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: whole-src-single
          AUTOBOT_SPECIALIST_OBJECTIVE: "Production Render lane: improve visible cinematic motion, transitions or finishing in src/cinematicRendererV3.js with one minimal, production-safe change. Preserve source decoding, recording and existing renderer APIs. Acceptance:
- the change improves a visible cinematic behavior without destabilizing render reliability
- only the owned file is changed
- syntax, build and product-quality verification pass
- leave a candidate when valid."
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs

      - name: Publish Render candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"

      - name: Independent QA + Reviewer Render candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/render-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/render-builder/autobot-candidate-review-render-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/render-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/render-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/render-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs render-builder
      - name: Upload Render worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-render-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/render-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/render-builder/autobot-candidate-review-render-builder.json
          if-no-files-found: ignore

  # MEDIA INTELLIGENCE — isolated production specialist owning the analysis-to-edit planning boundary.
  experimental-media-intelligence:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Media Intelligence lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock
      - name: Install Media Intelligence lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat
      - name: Start isolated Media Intelligence AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m
      - name: Run Media Intelligence specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: media-intelligence-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: diff-root-single
          AUTOBOT_SPECIALIST_OBJECTIVE: "Production Media Intelligence lane: improve analysis-to-edit planning quality in src/aiEditPlanner.js with one minimal, production-safe change. Preserve existing APIs and source media behavior. Acceptance:
- source evidence or cinematic quality contributes more meaningfully to edit planning
- only the owned file is changed
- syntax, build and product-quality verification pass
- leave a candidate when valid."
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs
      - name: Publish Media Intelligence candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"
      - name: Independent QA + Reviewer Media Intelligence candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/media-intelligence-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/media-intelligence-builder/autobot-candidate-review-media-intelligence-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/media-intelligence-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/media-intelligence-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/media-intelligence-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs media-intelligence-builder
      - name: Upload Media Intelligence worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-media-intelligence-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/media-intelligence-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/media-intelligence-builder/autobot-candidate-review-media-intelligence-builder.json
          if-no-files-found: ignore

  # CAPTION — isolated production specialist owning the speech-caption editorial boundary.
  experimental-caption:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Caption lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock
      - name: Install Caption lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat
      - name: Start isolated Caption AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m
      - name: Run Caption specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: caption-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: diff-root-single
          AUTOBOT_SPECIALIST_OBJECTIVE: "Production Caption lane: improve caption-safe overlay handling in src/captionPlanner.js with one minimal, production-safe change. Preserve verified speech timing and never invent dialogue. Acceptance:
- caption text remains time-aligned, confidence-aware and visually safe
- only the owned file is changed
- syntax, build and product-quality verification pass
- leave a candidate when valid."
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs
      - name: Publish Caption candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"
      - name: Independent QA + Reviewer Caption candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/caption-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/caption-builder/autobot-candidate-review-caption-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/caption-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/caption-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/caption-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs caption-builder
      - name: Upload Caption worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-caption-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/caption-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/caption-builder/autobot-candidate-review-caption-builder.json
          if-no-files-found: ignore

  # FAN-IN — records all ten isolated specialist lanes into the same
  # coordination ledger. It never merges product code and never gates the
  # production persistent-autobot job.
  experimental-intent:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Intent lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock
      - name: Install Intent lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat
      - name: Start isolated Intent AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m
      - name: Run Intent specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: intent-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: whole-src-single
          AUTOBOT_SPECIALIST_OBJECTIVE: |
            Production Creative Intent lane: improve prompt-to-edit intent compilation in src/creativeIntentCompiler.js with one minimal, production-safe change. Preserve existing APIs and contracts.
            Acceptance:
            - creative prompts are compiled into explicit, deterministic intent signals used by the production planning path
            - only the owned file is changed
            - syntax, build and product-quality verification pass
            - leave a candidate when valid.
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs
      - name: Publish Intent candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"
      - name: Independent QA + Reviewer Intent candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/intent-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/intent-builder/autobot-candidate-review-intent-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/intent-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/intent-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/intent-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs intent-builder
      - name: Upload Intent worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-intent-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/intent-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/intent-builder/autobot-candidate-review-intent-builder.json
          if-no-files-found: ignore

  # FAN-IN — records all ten isolated specialist lanes into the same
  # coordination ledger. It never merges product code and never gates the
  # production persistent-autobot job.

  experimental-continuity:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Continuity lane dependencies
        run: npm install --no-audit --no-fund --no-package-lock
      - name: Install Continuity lane Aider
        run: python -m pip install --disable-pip-version-check --no-input aider-chat
      - name: Start isolated Continuity AI brain
        run: bash scripts/autobot/install-local-brain.sh
        env:
          LOCAL_AI_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_DISCOVERY_MODEL: ${{ env.AUTOBOT_DISCOVERY_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          LOCAL_AI_PROXY_NUM_CTX: 4096
          LOCAL_AI_PROXY_NUM_PREDICT: 900
          OLLAMA_CONTEXT_LENGTH: 4096
          OLLAMA_NUM_PARALLEL: 1
          OLLAMA_MAX_QUEUE: 4
          OLLAMA_LOAD_TIMEOUT: 20m
          OLLAMA_KEEP_ALIVE: 90m
      - name: Run Continuity specialist
        id: execute
        env:
          AUTOBOT_SPECIALIST_BOT_ID: continuity-builder
          AUTOBOT_SPECIALIST_EDIT_STRATEGY: whole-src-single
          AUTOBOT_SPECIALIST_OBJECTIVE: |
            Production Creative Continuity lane: improve source/story continuity scoring in src/creativeContinuityEngine.js with one minimal, production-safe change. Preserve existing APIs and contracts.
            Acceptance:
            - continuity decisions preserve source identity and story consistency across the production edit path
            - only the owned file is changed
            - syntax, build and product-quality verification pass
            - leave a candidate when valid.
          AUTOBOT_SPECIALIST_BUILDER_ENABLED: 'true'
          AUTOBOT_EXPERIMENTAL_WORKER: 'false'
          AUTOBOT_BASE_REF: ${{ github.sha }}
          AUTOBOT_AIDER_MODEL: ${{ env.LOCAL_AI_MODEL }}
          AUTOBOT_SPECIALIST_PRODUCT_QUALITY_CHECK: npm run verify:autobot-product-change-quality
          AUTOBOT_SPECIALIST_FALLBACK_MODEL: ${{ env.AUTOBOT_SPECIALIST_FALLBACK_MODEL }}
          AUTOBOT_SPECIALIST_FALLBACK_MINUTES: '4'
          AUTOBOT_FINISH_GRACE_MINUTES: '0'
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          BUILDER_MAX_MINUTES: ${{ inputs.duration == '15m' && '15' || inputs.duration == '30m' && '30' || inputs.duration == '1h' && '60' || inputs.duration == '4h' && '60' || '60' }}
          AUTOBOT_COORDINATION_ID: persistent-${{ github.run_id }}
        run: node builder/runner/autobot-specialist-builder.mjs
      - name: Publish Continuity candidate branch
        if: steps.execute.outcome == 'success'
        run: |
          b="$(node -e "console.log(require('./builder/working/autobot-specialist-handoff.json').branch)")"
          git push --set-upstream origin "$b"
      - name: Independent QA + Reviewer Continuity candidate
        if: steps.execute.outcome == 'success'
        env:
          AUTOBOT_EXPECTED_CYCLE_BASE_COMMIT: ${{ github.sha }}
          AUTOBOT_SKIP_NPM_INSTALL: 'true'
          AUTOBOT_SPECIALIST_RESULTS_ROOT: builder/working/specialist-results
          AUTOBOT_CANDIDATE_CHECK_OUTPUT: builder/working/specialist-results/continuity-builder/autobot-endurance-candidate-check.json
          AUTOBOT_CANDIDATE_REVIEW_OUTPUT: builder/working/specialist-results/continuity-builder/autobot-candidate-review-continuity-builder.json
        run: |
          set -euo pipefail
          mkdir -p "builder/working/specialist-results/continuity-builder"
          cp builder/working/autobot-specialist-handoff.json "builder/working/specialist-results/continuity-builder/autobot-specialist-handoff.json"
          cp builder/working/autobot-specialist-outcome.json "builder/working/specialist-results/continuity-builder/autobot-specialist-outcome.json"
          node builder/runner/autobot-endurance-candidate-check.mjs continuity-builder
      - name: Upload Continuity worker evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-continuity-${{ github.run_id }}
          path: |
            builder/working/autobot-specialist-handoff.json
            builder/working/autobot-specialist-outcome.json
            builder/working/autobot-specialist-failure.patch
            builder/working/specialist-results/continuity-builder/autobot-endurance-candidate-check.json
            builder/working/specialist-results/continuity-builder/autobot-candidate-review-continuity-builder.json
          if-no-files-found: ignore

  # FAN-IN — records all ten isolated specialist lanes into the same
  # coordination ledger. It never merges product code and never gates the
  # production persistent-autobot job.

  experimental-fan-in:
    needs: [experimental-director, experimental-timeline, experimental-music, experimental-scene, experimental-rhythm, experimental-render, experimental-media-intelligence, experimental-caption, experimental-intent, experimental-continuity]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Download Director evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-director-${{ github.run_id }}
          path: builder/working/experimental/director

      - name: Download Timeline evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-timeline-${{ github.run_id }}
          path: builder/working/experimental/timeline

      - name: Download Music evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-music-${{ github.run_id }}
          path: builder/working/experimental/music

      - name: Download Scene evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-scene-${{ github.run_id }}
          path: builder/working/experimental/scene

      - name: Download Rhythm evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-rhythm-${{ github.run_id }}
          path: builder/working/experimental/rhythm

      - name: Download Media Intelligence evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-media-intelligence-${{ github.run_id }}
          path: builder/working/experimental/media-intelligence

      - name: Download Caption evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-caption-${{ github.run_id }}
          path: builder/working/experimental/caption

      - name: Download Render evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-render-${{ github.run_id }}
          path: builder/working/experimental/render

      - name: Download Intent evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-intent-${{ github.run_id }}
          path: builder/working/experimental/intent

      - name: Download Continuity evidence
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          name: autobot-persistent-experimental-continuity-${{ github.run_id }}
          path: builder/working/experimental/continuity

      - name: Build canonical fan-in ledger
        env:
          COORDINATION_ID: persistent-${{ github.run_id }}
        run: |
          set -euo pipefail
          mkdir -p builder/working/fan-in
          for bot in director timeline music scene rhythm render media-intelligence caption intent continuity; do
            dir="builder/working/experimental/$bot"
            handoff="$(find "$dir" -name autobot-specialist-handoff.json -print -quit 2>/dev/null || true)"
            outcome="$(find "$dir" -name autobot-specialist-outcome.json -print -quit 2>/dev/null || true)"
            if [ -z "$handoff" ] && [ -z "$outcome" ]; then
              cat > "$dir/autobot-specialist-outcome.json" <<EOF
          {
            "botId": "${bot}-builder",
            "status": "failure",
            "category": "workflow-failure",
            "repairable": true,
            "coordinationId": "$COORDINATION_ID",
            "error": "No worker evidence artifact was produced."
          }
          EOF
              outcome="$dir/autobot-specialist-outcome.json"
            fi
            if [ -n "$handoff" ]; then
              node builder/runner/autobot-independent-fan-in.mjs \
                --coordination-id "$COORDINATION_ID" \
                --handoff "$handoff" \
                --outcome "$outcome" \
                --ledger builder/working/fan-in/autobot-fan-in-ledger.json
            else
              node builder/runner/autobot-independent-fan-in.mjs \
                --coordination-id "$COORDINATION_ID" \
                --outcome "$outcome" \
                --ledger builder/working/fan-in/autobot-fan-in-ledger.json
            fi
          done
          cat builder/working/fan-in/autobot-fan-in-ledger.json

      - name: Build canonical candidate QA + Reviewer ledger
        if: always()
        run: |
          set -euo pipefail
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const bots=['director','timeline','music','scene','rhythm','render','media-intelligence','caption','intent','continuity'];
          const root='builder/working/experimental';
          const workers=[];
          for (const bot of bots) {
            const dir=`${root}/${bot}`;
            const read=(name)=>{const p=`${dir}/${name}`;try{return fs.existsSync(p)?JSON.parse(fs.readFileSync(p,'utf8')):null;}catch(error){return {status:'invalid-evidence',error:String(error.message||error)};}};
            const handoff=read('autobot-specialist-handoff.json');
            const outcome=read('autobot-specialist-outcome.json');
            const candidateCheck=read('autobot-endurance-candidate-check.json');
            const review=read(`autobot-candidate-review-${bot}-builder.json`) || read(`autobot-candidate-review-${bot}.json`);
            workers.push({
              botId:handoff?.botId||outcome?.botId||`${bot}-builder`,
              candidateCommit:handoff?.candidateCommit||candidateCheck?.candidateCommit||null,
              candidateOrigin:handoff?.candidateOrigin||outcome?.candidateOrigin||null,
              aiderMaterialized:handoff?.aiderMaterialized===true||outcome?.aiderMaterialized===true,
              specialistStatus:handoff?.status||outcome?.status||'missing',
              qaStatus:candidateCheck?.status||'not-run',
              integrationEligible:candidateCheck?.integrationEligible===true,
              reviewerStatus:review?.status||'not-run',
              reviewFindings:Array.isArray(review?.findings)?review.findings:[],
              reviewEvidencePresent:Boolean(review),
              candidateCheckEvidencePresent:Boolean(candidateCheck)
            });
          }
          const ledger={schemaVersion:'autobot-candidate-review-ledger-v1',coordinationId:`persistent-${process.env.GITHUB_RUN_ID}`,generatedAt:new Date().toISOString(),workers};
          fs.writeFileSync('builder/working/fan-in/autobot-candidate-review-ledger.json',JSON.stringify(ledger,null,2)+'\n');
          console.log(JSON.stringify(ledger,null,2));
          NODE

      - name: Upload unified ten-specialist fan-in evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: autobot-persistent-experimental-fan-in-${{ github.run_id }}
          path: |
            builder/working/fan-in/autobot-fan-in-ledger.json
            builder/working/fan-in/autobot-candidate-review-ledger.json
          if-no-files-found: ignore
